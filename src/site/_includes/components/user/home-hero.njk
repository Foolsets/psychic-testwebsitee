{% if dgHome %}
<div class="dg-home">
  <!-- ASCII Skater -->
  <div class="ascii-skater-container">
    <div class="ascii-skater" id="skater">🛹</div>
  </div>
  
  <section class="dg-home-hero">
    <!-- Плавающие элементы -->
    <div class="floating-item">[====]</div>
    <div class="floating-item">Ollie</div>
    <div class="floating-item">Kickflip</div>
    <div class="floating-item">[====]</div>
    
    <div class="hero-icon">☠️</div>
    
    <h1 class="hero-title">
      <span class="line1">WELCOME TO</span>
      <span class="line2">MY DIGITAL</span>
      <span class="line3">GARDEN</span>
    </h1>
    
    <div class="terminal-window">
      <div class="terminal-header">
        <div class="dot"></div>
        <div class="dot"></div>
        <div class="dot"></div>
        <span class="terminal-title">root@skate-os:~</span>
      </div>
      <div class="terminal-body">
        <div id="terminal-text"></div>
      </div>
    </div>
    
    <div class="hero-links">
      <a href="/ai-website">AI Articles</a>
      <a href="/about">About</a>
      <a href="/graph">Graph</a>
      <a href="https://github.com">GitHub</a>
    </div>
  </section>
  
  <section class="articles-section">
    <h2 class="section-title">Latest Posts</h2>
    <div class="articles-grid">
      <article class="article-card" onclick="location.href='/ai-website/animate-diff'">
        <h3 class="article-title">Animate Diff</h3>
        <p>AI animation techniques...</p>
        <span class="article-date">24/Feb/2026</span>
      </article>
    </div>
  </section>
</div>

<script>
// Terminal typing
const lines = [
  '> initializing skate_mode...',
  '> loading neon shaders... [OK]',
  '> system ready. welcome!'
];
let lineIdx = 0;
let charIdx = 0;
const term = document.getElementById('terminal-text');

function typeLine() {
  if (lineIdx < lines.length) {
    if (charIdx < lines[lineIdx].length) {
      term.innerHTML += lines[lineIdx][charIdx];
      charIdx++;
      setTimeout(typeLine, 30);
    } else {
      term.innerHTML += '<br>';
      lineIdx++;
      charIdx = 0;
      setTimeout(typeLine, 300);
    }
  }
}
setTimeout(typeLine, 500);
</script>
{% endif %}